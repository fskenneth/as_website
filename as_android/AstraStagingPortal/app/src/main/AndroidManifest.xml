<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Gallery access for the in-chat photo/video picker. On API 33+ we
         use the typed READ_MEDIA_* permissions; older OS levels fall back
         to READ_EXTERNAL_STORAGE (capped at 32 so it isn't requested on
         newer devices that don't honor it). -->
    <!-- The chat gallery icon launches Android's system Photo Picker
         (PickVisualMedia), which is permission-free and exposes the full
         on-device library, so no READ_MEDIA_* permissions are needed. -->

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:label="Astra Staging Portal"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
